spring:
  profiles:
    active: native
  application:
    name: configserver-service
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
server:
  port: 8888